/**
 * ⚠️ **本文件已作废（GONE）** —— 内容搬走了，别再用，也别照着它写。
 *
 * 这是**生成器式**的那版单位行为层：一个实现一个子类，`yield <绝对时刻>` 等价
 * `WaitForAge`。用户否掉了这个形状（"状态的形状先数出来、几台小机器就够"），
 * 现在的行为层是 `core/src/model/behaviors/` 下的几台**参数驱动**的机器：
 *
 * | 换成什么 | 文件 |
 * | --- | --- |
 * | 部署 / 撤收（属于**单位**，不属于武器） | `behaviors/deploy-machine.ts` |
 * | 持续效果（DoT / 回血 / 火 / 毒气） | `behaviors/effect-machine.ts` |
 * | 时间线记录（状态段 + 事件，两层共用） | `../timeline.ts` |
 *
 * 各机器的驱动是**调用方**的事 —— `Battle` 不再持有 `behaviors[]`（见 `../battle.ts`）。
 *
 * ⚠️ 这个空壳文件还在只是因为**删除需要一次命令批准**（`Remove-Item` 被沙箱拦下），
 * 批准到手就删；它**不导出任何东西**，留着不会影响编译。
 */
export {};
