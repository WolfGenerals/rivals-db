# 统一换行符为 LF，保证产物逐字节可复现。
#
# 仓库里既有源码也有生成的数据（data/*.json）。数据文件要求跨平台
# 逐字节一致，否则 Windows 与 Linux 上提取出的结果 diff 会不同。
# 这里显式指定，避免依赖开发者本地的 core.autocrlf 设置。
* text=auto eol=lf

# 二进制资产（不入库，仅防御性声明）
*.apk  binary
*.apk.1 binary
*.ttf  binary
*.pac  binary
*.sb   binary
*.sb3d binary
*.sba  binary
*.pb   binary
*.png  binary
