{
  "name": "rivals-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "web": "pnpm --filter @rivals/web dev",
    "web:build": "pnpm --filter @rivals/web build",
    "cli": "pnpm --filter @rivals/cli start",
    "extract": "pnpm --filter @rivals/cli start extract tmp/com.ea.gp.candcwarzones",
    "deploy": "pnpm --filter @rivals/web build && gh-pages -d web/dist --nojekyll"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.9.3"
  }
}
